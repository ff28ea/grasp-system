# Core scientific stack
# numpy 1.26.4 is the version actively tested in CI and on the
# reference workstation; the bound below allows any recent 1.2x/1.3x
# patch release so integrators can track their distro's security
# updates without editing this file.
numpy>=1.24,<2.0
scipy>=1.10

# Computer vision
# opencv-contrib-python is a superset of opencv-python; installing both causes
# mixed-namespace conflicts at runtime. Pin only the contrib wheel.
opencv-contrib-python>=4.8

# RealSense D435i
pyrealsense2>=2.54.2

# 3D / point clouds
open3d>=0.17

# Instance segmentation
ultralytics>=8.1
torch>=2.0
torchvision>=0.15

# PiPER arm SDK (install matching version for your hardware)
piper_sdk>=0.3

# Misc utilities
pyyaml>=6.0
tqdm>=4.66
matplotlib>=3.7
